# TODO: Backend Preprocessor Rework Plan

## Purpose
This file is the active TODO context for finishing the new `document_blocks -> document_operations -> simulation_steps -> Pre` pipeline.

It intentionally replaces older stage plans. Earlier data-model cleanup, YAML removal, `document_operations`, `simulation_steps` sibling rows, Operations-screen parse diagnostics, and semantic Pre adapter migration are part of the current baseline unless source code proves otherwise.

## Current Baseline
- Editable document source data lives in `document_blocks`.
- `document_operations` is regenerated from saved `document_blocks` and stores final user/process operation JSON in `operation_parameters`.
- `document_operations` no longer stores inherited/effective namespace columns.
- Deformation values are copied into generated Operation rows by direct-parent rules only.
- `simulation_steps.document_operation_id` is the primary key and required FK to `document_operations.document_operation_id`.
- Operation regeneration creates/removes sibling `simulation_steps` rows together with `document_operations`.
- Valid `document_operations` rows set sibling `simulation_steps.preprocess_ready = true`.
- Runtime YAML/schema files were removed; rules are localized in the endpoint/service code that executes them.
- Operations view shows `document_operations.operation_parameters` JSON and parse diagnostics.
- The Pre bridge currently reads `document_operations.operation_parameters` through `build_document_operation_outputs_for_document_version`; the old `ProcessCard` name has been replaced by `DocumentOperationOutput`.
- Semantic Pre definitions live in `backend/app/services/preprocessor/control_program_builder.py`.
- Pre compiler dispatch is semantic-template based, not old numeric operation-id based.
- `backend/scripts/check_preprocessor_pipeline.py --list-support` reports semantic adapter coverage.
- All current semantic operation templates have a real Pre adapter; no current template is intentionally left on generic fallback.
- API, Pre, Post, and Coordinator have local JSONL rotating-file logging and a frontend Logs view. Solver logging is intentionally excluded from this local log viewer for now.
- The document editor has explicit `Preprocessor` and `Postprocessor` inline result toggles near Save. Normal editing stays compact when both toggles are off. With `Preprocessor` enabled, the editor shows a single context panel under the active or single selected block, filters to direct source rows for that Operation/Furnace/Heating/Deformation context, uses last-row fallback for multi-row Operation blocks, and reuses the exact Steps 2D/3D side-by-side preview components. The `Postprocessor` toggle reserves the UI path with an explicit unavailable message until Post migration creates real result payloads.

## Current Main Risk
The remaining integration risk is not generic adapter coverage. The main risk is that Pre output is only partially represented in a user-friendly way for expert inspection, advanced multi-step visual analysis is not implemented yet, and some downstream migrated math may still assume old-project DB/table/column shapes, old parameter names, or old wide-table payloads.

The first work item remains the Simulations/Steps inspection UI plus correct 2D/3D geometry display. Backend runtime validation remains the next stage and continues to verify the real worker path against the current DB schema and JSON payloads.

## How To Reference This Plan
- Say `этап 1`, `этап 2`, etc. to work on a full stage.
- Say `задача 1.3` to reference a specific task.
- Say `следующий этап по TODO_BACKEND_PREPROCESSOR_REWORK` to continue from the next unfinished stage.
- If implementation changes make this file wrong, update this context file in the same task.
- When showing tasks to the user, use task ids as the only numbering, for example `1.10 Single-Step 3D Overlay Mode`.

## Decisions Already Made
- Do not restore the old `server_pre_main` wide-table schema as-is.
- Do not add a new all-in-one central rules file.
- Keep rule ownership localized in the backend/frontend code that executes the rule.
- Keep important stable workflow/query fields as typed columns.
- Keep operation-specific, variable, and evolving Pre payloads in JSONB unless a field becomes a stable query/index key.
- Keep large arbitrary binary/heavy artifacts out of the hot list-view path. Store only compact summaries or references in `simulation_steps` when payloads become large.
- Keep `simulation_steps` focused on Pre-computed runtime output and stable Solver/Post keys. This means every value generated by the old Pre path must be generated by the new Pre path and stored in `simulation_steps`, unless it is intentionally replaced by an equivalent new Pre output. Cleanup must never remove real old-Pre-generated values.
- Do not turn `simulation_steps` into a copy of inherited/source document parameters. User-entered/source values should stay in `document_operations` and can be joined into API responses only when useful for explanation.
- Keep ID data in `simulation_steps` minimal: required sibling/source/version IDs, stable Solver/Post FK references, and traceability IDs only. Do not duplicate IDs inside JSON payloads when typed columns already carry them.
- Keep status data in `simulation_steps` sufficient for troubleshooting: readiness, pending/compiled/failed state, stale/missing-artifact state, worker/run timestamps where needed, and actionable error payloads. Avoid duplicating the same status in several JSON locations.
- Use local JSONL files plus read-only API endpoints for API/Pre/Post/Coordinator diagnostics. Do not store diagnostic logs in workflow tables.
- Naming cleanup rule: old `process` terms that mean technological process/document flow should become `document`; old materialized-operation input wording should become `document_operation`. Do not rename `preprocess`, `preprocessor`, `postprocessing`, OS/runtime process terminology, or UI card styling terms such as `ui-card`.
- Final chosen target for the old `ProcessCard` artifact is now implemented: `DocumentOperationOutput`. In the current architecture this name means the output of the `document_blocks -> document_operations` materialization stage as consumed by Pre, not the final Pre output.
- Backend forging math should use an engineer-readable calculation-sheet style. Keep semantic-template dispatch and compatibility mapping outside math functions; keep one top-level calculation function per operation variant; keep 2D contour generation and 3D surface generation separate from scalar engineering formulas; use typed inputs/results with explicit units where practical; and convert typed math results to JSON/dict payloads only at compiler/API boundaries.

## Deferred Or Rejected Items
- Do not add a duplicate Operations table elsewhere in the UI. Extend the existing Operations workspace instead.
- Do not add per-row Pre result placeholders inside the document by default. Result areas appear only when result display mode is explicitly enabled.
- Do not implement a full raw `simulation_steps` table mode as the user-facing Pre result view. It is an explicitly rejected UI option because it forces users to read a wide DB-shaped table and scroll both horizontally and vertically. The Steps workspace should be a compact expert dashboard where graphics are primary and tables are supporting. Keep raw JSON/details available only as targeted expandable diagnostics for the selected step.
- Do not calculate and color a true 3D intersection volume for initial/final overlays unless the chosen 3D viewer provides it cheaply. Use transparent overlap instead.
- Do not implement physically exact rotation animation until `simulation_steps` carries enough orientation/basis metadata to avoid guessing.
- Do not add a new orchestration framework. Continue with lightweight worker processes plus PostgreSQL-backed queue/notification flow.
- Do not migrate Solver/Post until the Pre path is validated on real documents and `simulation_steps` payload shape is stable enough.
- Do not expose Solver raw log files through the central frontend Logs view until Solver-PC deployment/log transport is designed.

## Revised Layer-Ordered Stages
The remaining work is grouped by product layer and migration dependency order:
Frontend first, then API, Pre, Solver, Post, and verification/migration support.

Only stages with open or deferred work are listed. Completed stage IDs are intentionally omitted and remaining stage IDs are not renumbered.

## Stage 0: Simplify Steps Dashboard Inputs
Goal: make the Steps dashboard readable before adding more advanced charts by reducing noisy data and using semantic visual state indicators.

Status: open.

Task `0.1` inventory result: `.codex/context/PRE_OUTPUT_INVENTORY.md`.

Tasks:
- 0.1 Done: inventory old Pre-generated values and map each one to the new `simulation_steps` payload. Generated geometry, dimensions, areas/volumes, deformation metrics, feed/speed/rotation/control-program values, artifact metadata, and other old Pre outputs must remain available from `simulation_steps`.
- 0.2 Clean only unnecessary duplicated inputs from `simulation_steps`: accidental copies of inherited/source document, heating, deformation, furnace, operation, raw operation parameters, and compatibility dumps that are not generated by Pre.
- 0.3 Keep source/user-entered parameters in `document_operations`; do not include them in the default Steps payload. Expose them only through the Operations view or a future explicit trace action.
- 0.4 Minimize ID data in `simulation_steps`: keep required sibling/source/version IDs and stable FK references used by Solver/Post/UI traceability; remove duplicated IDs from JSON payloads when columns already hold them.
- 0.5 Keep status data in `simulation_steps`: readiness, pending/compiled/failed state, stale/missing-artifact state, worker/run timestamps where needed, and actionable error payloads.
- 0.6 Add compact icon rendering for trigger variables, boolean statuses, and semantic status variables in the Steps dashboard.
- 0.7 Use vector icons with tooltips instead of long text where the state meaning is clear.
- 0.8 Add deformation chart groups for relative, logarithmic, and absolute deformation by axes, plus effective/intensity deformation when Pre provides it.
- 0.9 Done: separate selected-step troubleshooting evidence into API diagnostics and lazily loaded related Pre log records; keep actual log records out of the hot `simulation_steps` list response.

Exit criteria:
- The Steps dashboard shows fewer raw/noisy source duplicates, preserves all old-Pre-generated outputs in `simulation_steps`, separates status/API/log evidence by provenance, and makes status/trigger fields readable without requiring wide JSON tables.

## Stage 1: Frontend Steps Tester Workspace
Goal: make `simulation_steps` readable enough that users can participate in Pre testing before Solver migration.

Status: partially done.

Tasks:
- 1.1 Add multi-step selection in the left Steps list.
- 1.2 Keep one active step for detail panels while allowing multiple selected steps for comparison.
- 1.3 Add multi-step mode `Initial first + final last`; make this mode global for 2D, 3D, metric tables, and metric charts.
- 1.4 Add multi-step mode `All final forms`; make it available for 2D/3D and metric charts when the selected payloads support it.
- 1.5 Add multi-step mode `Animation` only after Stage 10 orientation payload exists.
- 1.6 Improve 2D multi-step overlays with optional bounds/reference rectangles and compact legends.
- 1.7 Add copy/export control for selected-step summary.
- 1.8 Add copy/export control for selected-step diagnostics JSON.
- 1.9 Add copy/download control for 2D SVG/image when browser support allows.
- 1.10 Add copy/download control for 3D snapshot or artifact links when available.
- 1.11 Keep all failures visible as explicit messages; never create visual fallback geometry when Pre artifacts are missing.
- 1.12 Add a compact metric-dashboard layer to the Steps detail workspace. Graphics are primary; tables remain compact supporting details.
- 1.13 In single-step mode, add bar-chart style comparisons for initial vs final values: H/W/L, cross-section area, volume, surface area, and available deformation metrics.
- 1.14 In multi-step mode, add line-chart style trends over selected steps for geometry dimensions, cross-section area, volume, surface area, accumulated deformation, and available strain/deformation metrics.
- 1.15 Add linear/log vertical-axis toggle for dimensional and integral metric charts where scale differences make trends hard to read.

Exit criteria:
- A tester can open a document, run Pre, pick any step, see readable 2D/3D geometry, see source user parameters, and understand whether a row is valid, pending, failed, or missing artifact data.

## Stage 5: API Result And Diagnostics Contract
Goal: make frontend Steps, Logs, and user-facing issue views consume stable API payloads instead of guessing arbitrary JSON.

Status: partially done.

Tasks:
- 5.1 Standardize compact result fields: `document_operation_id`, source block/operation label, status/severity, short summary, detailed diagnostics, geometry summary, and optional artifact references.
- 5.2 Add API-level issue summary for selected document/version.
- 5.3 Add stable machine-readable issue code for parse errors.
- 5.4 Add stable machine-readable issue code for missing required parameters.
- 5.5 Add stable machine-readable issue code for Pre compile failures.
- 5.6 Add stable machine-readable issue code for missing artifacts.
- 5.7 Add stable machine-readable issue code for stale output.
- 5.8 Add stable machine-readable issue code for unsupported operations.
- 5.9 Keep raw JSON payloads available, but not as the default user-facing mode.
- 5.10 Add a stable compact metric summary for dashboard charts: geometry dimensions, geometry integrals, deformation-by-axis, effective/intensity deformation, and state/trigger flags.
- 5.11 Keep API payloads explicitly separated into source/user parameters versus Pre-computed step output, so the frontend can explain a row without encouraging inherited/source fields to be duplicated inside `simulation_steps`.

Exit criteria:
- Frontend can show Steps, Logs, and error/warning UX without inferring meaning from ad hoc payload keys.

## Stage 6: API Workflow Control
Goal: expose safe user actions for regeneration, Pre retry, and status inspection.

Status: partially done.

Tasks:
- 6.1 Add explicit status endpoint for the latest document version, including operation regeneration status, Pre queued/running/failed/compiled status, worker name, and started/finished timestamps.
- 6.2 Ensure refresh/retry actions are idempotent and safe under repeated user clicks.
- 6.3 Add API response details that explain why retry cannot start when blocked.
- 6.4 Keep PostgreSQL queue/notification flow; do not add a heavy orchestration framework.

Exit criteria:
- A tester can safely trigger Pre retry and understand whether it started, failed to start, or is waiting.

## Stage 7: API Logging And Local Diagnostics
Goal: make local structured logging a stable support tool for API/Pre/Post/Coordinator.

Status: partially done.

Tasks:
- 7.1 Confirm every API endpoint used by Steps/Pre retry logs enough context on error.
- 7.2 Add request/action correlation identifiers where practical: document id, document version id, document operation id, worker name, and operation template id.
- 7.3 Keep logs local files; do not store diagnostic logs in workflow DB tables.

Exit criteria:
- Logs are sufficient to diagnose user-reported Steps/Pre retry problems without debugger access.

## Stage 8: Pre Runtime Validation
Goal: prove that the current JSON Pre path works through the real worker, current DB schema, current parameter names, and current `simulation_steps` sibling rows.

Status: partially implemented in `backend/scripts/check_preprocessor_pipeline.py`.

Validation command from `backend/`:
`.venv/bin/python scripts/check_preprocessor_pipeline.py --validate-stage1 --worker-once`

Note: the command keeps historical `--validate-stage1` naming even though this is now Stage 8.

Tasks:
- 8.1 Re-run validation after every meaningful Pre/compiler/schema change.
- 8.2 Confirm a long-running Pre worker starts, idles, wakes, claims, and handles missed notifications.
- 8.3 Confirm `build_document_operation_outputs_for_document_version` remains the only DB-to-Pre bridge.
- 8.4 Confirm no path reads old `operations`, `server_pre_main`, `process_versions`, or numeric operation-type tables.

Exit criteria:
- Real Pre worker can start, claim a queued document, compile a valid document, report a controlled invalid row, and stop retry loops on failure.

## Stage 9: Pre Parameter Compatibility And Data Contract
Goal: make the Pre compiler consume current JSON payloads without old wide-table coupling.

Status: partially done.

Tasks:
- 9.1 Audit semantic parameter names for billet/document initial data.
- 9.2 Audit semantic parameter names for material id/name/version.
- 9.3 Audit semantic parameter names for input stock geometry type and dimensions.
- 9.4 Audit semantic parameter names for input stock weight/volume.
- 9.5 Audit semantic parameter names for mesh element count.
- 9.6 Audit semantic parameter names for press id and press mode id.
- 9.7 Audit semantic parameter names for die assembly id, top die id, bottom die id, and die dimensions injected by DB enrichment.
- 9.8 Audit semantic parameter names for `speed_upsetting` and `speed_prolongation`.
- 9.9 Audit semantic parameter names for feed direction id, `feed_first`, `feed_middle`, and `feed_last`.
- 9.10 Audit semantic parameter names for parsed operation text/table values.
- 9.11 Prefer narrow adapters/aliases at the Pre bridge boundary over renaming frontend fields blindly.
- 9.12 Maintain mapping report: semantic operation id -> Pre adapter -> legacy operation/math source.
- 9.13 Maintain mapping report: new JSON parameter name -> old preprocessor/math variable name.
- 9.14 Fail loudly if a new semantic template would use generic fallback.
- 9.15 Done: introduced typed Pre math input/result models for cogging/prolongation variants under `backend/app/services/preprocessor/cogging/models.py`.
- 9.16 Done: split current mixed prolongation math into separate operation-variant modules under `backend/app/services/preprocessor/cogging/math/`: axial height/feed, axial height/bites, axial skipped-bites, spiral one-rotation, spiral three-rotations, radial height/feed, radial press-axis feed, radial height/bites, transverse all-in-one, and transversal rotation-height.
- 9.17 Done: branching by `operation_template_id` is isolated in narrow cogging adapters; compiler plumbing now calls the typed adapter, and individual operation-variant math functions do not branch across unrelated cogging types.

Exit criteria:
- Every current operation template has documented parameter mapping and a real adapter; cogging/prolongation math is reachable through typed, per-variant calculation functions rather than one mixed branch-heavy function.

## Stage 10: Pre Geometry, Artifacts, And Orientation Payload
Goal: keep Pre geometry useful for both expert inspection and future animation/Solver/Post without hidden fallback algorithms.

Status: partially done.

Tasks:
- 10.1 Normalize `initial_geometry.basis`.
- 10.2 Normalize `final_geometry.basis`.
- 10.3 Normalize `initial_geometry.top_marker`.
- 10.4 Normalize `final_geometry.top_marker`.
- 10.5 Normalize `calculations.rotation_sequence`.
- 10.6 Normalize `pre_output.rotation_sequence`.
- 10.7 Preserve old-project variable names only inside backend adapters.
- 10.8 Add diagnostics when animation or orientation-sensitive result display is requested but orientation payload is missing.
- 10.9 Decide whether persistent heavy artifacts need a dedicated artifact table before Solver/Post depend on them.
- 10.10 Done: split cogging/prolongation 2D contour generation into operation-specific contour functions under `backend/app/services/preprocessor/cogging/contours_2d/`; Shapely details remain behind that layer.
- 10.11 Done: split cogging/prolongation 3D surface generation into operation-specific surface functions under `backend/app/services/preprocessor/cogging/surfaces_3d/`; Trimesh behavior remains behind that layer and existing explicit missing-artifact behavior is preserved.
- 10.12 Done: scalar math, 2D contour, and 3D surface responsibilities are separated for cogging/prolongation.

Exit criteria:
- Frontend can trust geometry/orientation payloads and never invent orientation, rotation, or substitute meshes; backend cogging geometry is separated into scalar math, 2D contour, and 3D surface layers.

## Stage 11: Pre Fixtures And Regression Tests
Goal: keep operation materialization and Pre bridge stable while Solver/Post migration continues.

Status: open/partial.

Tasks:
- 11.1 Add regression tests for Operation text parsing.
- 11.2 Add regression tests for Rounding table parsing.
- 11.3 Add regression tests for Furnace table materialization.
- 11.4 Add regression tests for `document_initial_data` materialization and Pre compilation.
- 11.5 Add regression tests for direct-parent Deformation die copy rules.
- 11.6 Add regression tests for direct-parent Deformation feed copy rules.
- 11.7 Add regression tests for direct-parent Deformation speed copy rules.
- 11.8 Add regression tests for direct-parent Deformation parser-variable copy rules.
- 11.9 Add focused billet-first fixture for `document_initial_data -> NewBillet -> simulation_steps`.
- 11.10 Add 2-3 typical document fixtures covering Billet, Furnace, Upsetting, Cogging, Rounding, Radial, Transverse, and Cutting.
- 11.11 Verify fixed document/version immutability after document fixation.
- 11.12 Done: added per-variant cogging/prolongation regression coverage in `backend/tests/test_cogging_prolongation_math.py` using captured outputs from the old mixed `calculate_prolongation` behavior; the compatibility wrapper remains.
- 11.13 Partially done: added focused cogging 2D contour regression through final-geometry outputs and lightweight 3D surface-dispatch coverage; heavier persisted artifact-summary fixtures are still deferred until Pre artifact test harnesses are broader.

Exit criteria:
- A regression run catches accidental breaks in parsing, materialization, Pre compilation, cogging math/contour/surface calculations, and row-by-row `simulation_steps` output.

## Stage 12: Solver Input Contract
Goal: define the minimum `simulation_steps` payload Solver needs before migrating Solver workers.

Status: open.

Tasks:
- 12.1 Confirm each compiled simulation step contains feed/bite/rotation counts needed by Solver.
- 12.2 Confirm each compiled simulation step contains initial length and current billet geometry needed by Solver.
- 12.3 Confirm each compiled simulation step contains active operation template/kind needed by Solver.
- 12.4 Confirm each compiled simulation step contains selected press/press mode needed by Solver.
- 12.5 Confirm each compiled simulation step contains selected die assembly or separate top/bottom/side dies needed by Solver.
- 12.6 Confirm each compiled simulation step contains working speed and feed settings copied from direct parent Deformation.
- 12.7 Confirm each compiled simulation step contains timing fields and accumulated time.
- 12.8 Keep Solver input values in compact JSON payloads unless a field becomes a queue/index key.
- 12.9 Add validation diagnostics when Solver-required values are missing.
- 12.10 Keep operation rows visible for troubleshooting even when Solver-required values are missing.

Exit criteria:
- Solver can be migrated without querying editor blocks or depending on old wide Pre tables.

## Stage 13: Solver Worker Migration
Goal: migrate Solver execution after Pre output shape is stable.

Status: deferred until Stages 8-12 are stable.

Tasks:
- 13.1 Audit old Solver adapters that still expect old flat columns.
- 13.2 Update Solver adapters to consume `simulation_steps.pre_output`, required compact `pre_input`, and artifact references from `simulation_steps.calculations`.
- 13.3 Keep one Solver task equal to one `simulation_steps` row.
- 13.4 Respect priority and pause/stop/continue orchestration rules.
- 13.5 Add Solver worker status reporting for UI without exposing raw remote log files yet.
- 13.6 Design Solver-PC log transport separately before adding Solver logs to the frontend Logs view.

Exit criteria:
- Solver workers can execute one compiled simulation step row using only current `simulation_steps` contract plus file/artifact references.

## Stage 14: Post Result Contract
Goal: define what Post consumes and produces after Solver output is available.

Status: deferred.

Tasks:
- 14.1 Define postprocessing task input contract keyed by `document_operation_id`.
- 14.2 Define Post numeric summary outputs.
- 14.3 Define Post text note outputs.
- 14.4 Define Post 2D image outputs.
- 14.5 Define Post 3D image outputs.
- 14.6 Define Post report/PPT artifact outputs.
- 14.7 Store large Post artifacts outside hot workflow rows and keep compact references in DB.
- 14.8 Standardize Post warnings/errors to the same severity and issue-message model used by Steps/Pre.
- 14.9 Extend frontend document result toggles and Steps/details views to show Post output only when explicitly enabled.

Exit criteria:
- Post can run from Solver/step outputs and expose compact, user-readable results without bloating editor/default views.

## Stage 15: Post Worker Migration
Goal: migrate the old Post worker after Post input/output contract is stable.

Status: deferred.

Tasks:
- 15.1 Audit old Post worker code for old table/column dependencies.
- 15.2 Migrate report/image/PPT generation against new IDs and artifact references.
- 15.3 Connect Post worker to `postprocessing_tasks`.
- 15.4 Verify Post task priority follows process/simulation priority rules.
- 15.5 Add local Post logs to existing Logs view, already supported by local logging foundation.

Exit criteria:
- Post workers consume queued postprocessing tasks and produce artifacts tied to the current document operation / simulation step identity.

## Stage 16: Cross-Cutting Diagnostics And Developer Support
Goal: keep future Pre/Solver/Post migration safe without adding heavy orchestration.

Status: partial.

Tasks:
- 16.1 Add or maintain a developer diagnostic endpoint/script showing `document_blocks -> document_operations -> simulation_steps`.
- 16.2 Keep semantic adapter coverage visible with `check_preprocessor_pipeline.py --list-support`.
- 16.3 Keep old/new operation and parameter mapping reports current.
- 16.4 Add DB/API sanity check for sibling row existence.
- 16.5 Add DB/API sanity check for stale rows.
- 16.6 Add DB/API sanity check for missing artifacts.
- 16.7 Add DB/API sanity check for missing orientation metadata.
- 16.8 Add DB/API sanity check for failed worker rows.
- 16.9 Keep context files updated whenever implementation changes the active contracts.

Exit criteria:
- A developer can trace any visible frontend issue back through block, operation, simulation step, worker, and artifact layers.

## Stage 17: Legacy Naming Cleanup
Goal: remove old-project naming artifacts that now obscure the `document_blocks -> document_operations -> simulation_steps` architecture.

Status: partially done.

Rules:
- Apply `process -> document` only where `process` means old technological-process/document entity naming.
- Apply `document_operation` wording where old code meant a user operation or materialized operation input.
- Do not rename `preprocess`, `preprocessor`, `postprocessing`, OS/runtime process wording, frontend `ui-card`, or visual material/die/press cards.
- Do not rename `DocumentOperation` ORM model. It is the DB row model for `document_operations`.
- The old `ProcessCard` symbol has been renamed to `DocumentOperationOutput`. Treat it as the materialized output consumed by Pre.

Tasks:
- 17.1 Done: rename `ProcessCard` dataclass to `DocumentOperationOutput`.
- 17.2 Done: rename active compiler/runtime method and argument names to local `operation_output` / `document_operation_outputs`.
- 17.3 Done: rename `build_process_cards_for_document_version` to `build_document_operation_outputs_for_document_version`.
- 17.4 Done: rename active Pre user/developer-facing error text to `operation output operation_id=...`.
- 17.5 Done: rename active Pre/runtime log messages to document-operation-output wording.
- 17.6 Done: rename active Pre comments/docstrings that used old materialized-operation wording to document operation outputs.
- 17.7 Done: rename context docs references from raw old input wording to raw document-operation wording.
- 17.8 Done: rename `process_name` in active preprocessor metadata to `operation_display_name`.
- 17.9 Done: rename root `document_initial_data` namespace `process_data` to `production_data`; keep a temporary Pre bridge compatibility read for existing rows until documents are regenerated.
- 17.11 Done: delete unused frontend `OperationBlockTypeRecord` legacy type and remove unused `backend/scripts/operations.json` operation-library artifact.
- 17.10 Run `python3 -m compileall backend/app`, backend `.venv` compileall if needed, frontend `npm run typecheck`, and Pre smoke validation after the rename.

Exit criteria:
- No active backend compiler/orchestration code uses `ProcessCard` or card terminology for materialized operation rows.
- DB table names and ORM model names remain unambiguous: `DocumentOperation` is the persisted DB row; `DocumentOperationOutput` is the Pre bridge object derived from it.
- Remaining `process` and `card` occurrences are either intentional (`preprocess`, OS process, postprocessing, UI card) or explicitly documented compatibility names.

## Explicit Deferred Task
- D.1 Do not implement exact animation rotation until normalized `basis` / `rotation_sequence` data exists in `simulation_steps`.
